# dependencies / builds
node_modules/
.next/
out/

# convex local state + generated-by-deploy caches
.convex/

# environment: only .env.example is tracked
.env
.env.*
!.env.example

# logs / tooling
*.log
.DS_Store

# vercel CLI link
.vercel/

# lockfiles omitted: Windows-generated lock dropped the Linux lightningcss native binary
# Vercel resolves via `npm install` (see installCommand)
package-lock.json
